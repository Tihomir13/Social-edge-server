<!DOCTYPE html>
<html>
  <head>
    <style>
      body {
        margin: 0;
        padding: 0;
        font-family: Arial, sans-serif;
        background-color: linear-gradient(
          135deg,
          #89f6fe8d 10%,
          hsla(215, 100%, 70%, 0.4) 50%,
          #ffaee8 100%
        );
      }

      .email-container {
        max-width: 600px;
        margin: 0 auto;
        background-color: #fff;
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        text-align: center;
      }

      h1 {
        color: rgb(143, 30, 255);
      }

      p {
        font-size: 16px;
        color: #ffffff;
      }

      a {
        display: inline-block;
        margin-top: 20px;
        padding: 10px 20px;
        color: #fff;
        background-color:rgb(143, 30, 255);
        text-decoration: none;
        font-weight: bold;
        border-radius: 5px;
      }

      a:hover {
        background-color: rgb(200, 146, 255);
      }
    </style>
  </head>
  <body>
    <div class="email-container">
      <h1>Hello {{name}}!</h1>
      <p>Please confirm your email by clicking the link below:</p>
      <a href="{{url}}">Verify Email</a>
    </div>
  </body>
</html>
